// Placeholder app entry point so the packaging pipeline builds end to end.
// The integrator replaces this file with the real one from workstream D.

import SwiftUI

@main
struct JetlinkApp: App {
  var body: some Scene {
    WindowGroup {
      Text("Jetlink")
        .padding()
    }
  }
}
