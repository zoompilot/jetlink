// Placeholder test so the test target and the `make test` target are proven
// end to end. The integrator replaces this file with the real suites from
// workstreams C and D.

import Testing

@Test func placeholder() {
  #expect(1 + 1 == 2)
}
